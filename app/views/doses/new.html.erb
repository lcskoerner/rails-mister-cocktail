<%= simple_form_for [@cocktail, @dose] do |d| %>
  <%= d.association :ingredient, collection: Ingredient.order(:name),
  prompt: "Pick an ingredient", include_blank: false %>
  <%= d.input :description %>
  <%= d.button :submit, "Create dose", class: "btn btn-success" %>
<% end %>
